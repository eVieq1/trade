<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.salestracker">

    <!-- Разрешения -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:usesCleartextTraffic="true"
        android:requestLegacyExternalStorage="true">

        <!-- Главный экран входа -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Главное меню -->
        <activity
            android:name=".MainMenuActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Настройка планов / графика -->
        <activity
            android:name=".PlanSettingsActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Управление сотрудниками -->
        <activity
            android:name=".EmployeesActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Управление магазинами -->
        <activity
            android:name=".ShopsActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Управление сотрудниками в магазине -->
        <activity
            android:name=".ShopEmployeesActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Редактирование продаж -->
        <activity
            android:name=".EditSalesActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- FileProvider для экспорта файлов -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>